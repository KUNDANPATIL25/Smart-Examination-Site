services:
  - type: web
    name: ai-exam-backend
    env: other
    buildCommand: cd backend && ./mvnw clean install
    startCommand: java -jar backend/target/ai-exam-backend-1.0.0.jar
    envVars:
      - key: GEMINI_API_KEY
        sync: false
        
